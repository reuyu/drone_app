# 터미널 명령어 가이드

## 🚀 빠른 시작

### 1. UI 개발 서버 실행 (frontend_ui_dev)

```powershell
# UI 개발 폴더로 이동
cd C:\Users\user\Desktop\drone_app-main\drone_app-main\frontend_ui_dev

# 개발 서버 실행
npm run dev
```

**접속 주소**: http://localhost:5173

---

### 2. 백엔드 서버 실행 (선택사항)

```powershell
# 백엔드 폴더로 이동
cd C:\Users\user\Desktop\drone_app-main\drone_app-main\backend

# 서버 실행
npm start
```

**접속 주소**: http://localhost:3000

**참고**: 서버가 없어도 Mock 데이터로 UI 작업 가능

---

## 📁 경로 정리

### 현재 작업 디렉토리
```
C:\Users\user\Desktop\drone_app-main
```

### 주요 폴더 경로
```
drone_app-main/
├── drone_app-main/
│   ├── backend/              # 백엔드 서버
│   ├── frontend/             # 원본 프론트엔드
│   └── frontend_ui_dev/      # UI 작업용 폴더 ⭐
└── firedronui0101-1/         # UI 참고용 프로젝트
```

---

## 🔧 자주 사용하는 명령어

### UI 개발 서버
```powershell
# UI 개발 폴더로 이동
cd drone_app-main\drone_app-main\frontend_ui_dev

# 개발 서버 실행
npm run dev

# 빌드 (배포용)
npm run build

# 린트 체크
npm run lint
```

### 백엔드 서버
```powershell
# 백엔드 폴더로 이동
cd drone_app-main\drone_app-main\backend

# 서버 실행
npm start

# 개발 모드 (nodemon 사용)
npm run dev
```

### 의존성 설치
```powershell
# UI 개발 폴더
cd drone_app-main\drone_app-main\frontend_ui_dev
npm install

# 백엔드 폴더
cd drone_app-main\drone_app-main\backend
npm install
```

---

## 🎯 작업 시나리오

### 시나리오 1: UI만 작업 (서버 없음)
```powershell
# 1. UI 개발 폴더로 이동
cd C:\Users\user\Desktop\drone_app-main\drone_app-main\frontend_ui_dev

# 2. 개발 서버 실행
npm run dev

# 3. 브라우저에서 http://localhost:5173 접속
# → Mock 데이터로 자동 작동
```

### 시나리오 2: 서버와 함께 작업
```powershell
# 터미널 1: 백엔드 서버
cd C:\Users\user\Desktop\drone_app-main\drone_app-main\backend
npm start

# 터미널 2: 프론트엔드 서버
cd C:\Users\user\Desktop\drone_app-main\drone_app-main\frontend_ui_dev
npm run dev

# → 자동으로 서버 감지하여 실제 API 사용
```

---

## 📝 PowerShell 단축 명령어

### 현재 위치에서 바로 실행
```powershell
# UI 개발 서버 (현재 위치: drone_app-main)
cd drone_app-main\frontend_ui_dev; npm run dev

# 백엔드 서버 (현재 위치: drone_app-main)
cd drone_app-main\backend; npm start
```

---

## ⚠️ 문제 해결

### 포트가 이미 사용 중일 때
```powershell
# 포트 5173 사용 중인 프로세스 확인 (Windows)
netstat -ano | findstr :5173

# 포트 3000 사용 중인 프로세스 확인
netstat -ano | findstr :3000
```

### 캐시 클리어
```powershell
# npm 캐시 클리어
npm cache clean --force

# node_modules 재설치
rm -r node_modules
npm install
```

---

## 🔍 현재 실행 중인 서버 확인

### PowerShell에서 확인
```powershell
# 모든 Node.js 프로세스 확인
Get-Process node -ErrorAction SilentlyContinue

# 특정 포트 확인
netstat -ano | findstr :5173
netstat -ano | findstr :3000
```

