# 🚀 FireDrone 프로젝트 환경 설정 가이드

새로운 환경에서 프로젝트를 시작하기 위한 완전한 가이드입니다.

## 📋 필수 요구사항

### 1. Node.js 설치
- **버전**: Node.js 18 이상 (권장: 20.x LTS)
- **다운로드**: https://nodejs.org/
- **확인 방법**:
```bash
node --version
npm --version
```

### 2. 패키지 매니저
- **npm** (Node.js와 함께 설치됨) 또는 **yarn**
- **yarn 설치** (선택사항):
```bash
npm install -g yarn
```

### 3. Expo CLI 설치
```bash
npm install -g expo-cli
# 또는
npm install -g @expo/cli
```

### 4. Git (선택사항, 버전 관리용)
- **다운로드**: https://git-scm.com/

---

## 📦 프로젝트 의존성 설치

### 1. 프로젝트 폴더로 이동
```bash
cd firedrone
```

### 2. 모든 패키지 설치
```bash
npm install
```

또는 yarn 사용 시:
```bash
yarn install
```

### 설치되는 주요 패키지들:

#### 핵심 프레임워크
- `expo`: ~54.0.30 - Expo SDK
- `react`: 19.1.0 - React 라이브러리
- `react-native`: 0.81.5 - React Native 프레임워크

#### 네비게이션
- `@react-navigation/native`: ^7.1.26 - 네비게이션 핵심
- `@react-navigation/native-stack`: ^7.9.0 - 스택 네비게이션
- `@react-navigation/bottom-tabs`: ^7.4.0 - 하단 탭 네비게이션
- `react-native-screens`: ~4.16.0 - 네이티브 스크린
- `react-native-gesture-handler`: ~2.28.0 - 제스처 핸들러

#### Expo 모듈
- `expo-location`: ~19.0.8 - 위치 정보
- `expo-notifications`: ~0.32.15 - 푸시 알림
- `expo-av`: ~16.0.8 - 비디오/오디오 재생
- `expo-image`: ~3.0.11 - 이미지 최적화
- `expo-status-bar`: ~3.0.9 - 상태바 제어
- `expo-splash-screen`: ~31.0.13 - 스플래시 스크린
- `expo-constants`: ~18.0.12 - 앱 상수
- `expo-font`: ~14.0.10 - 폰트 관리
- `expo-haptics`: ~15.0.8 - 햅틱 피드백
- `expo-linking`: ~8.0.11 - 딥링크
- `expo-web-browser`: ~15.0.10 - 웹 브라우저
- `expo-system-ui`: ~6.0.9 - 시스템 UI

#### UI 라이브러리
- `lucide-react-native`: ^0.562.0 - 아이콘 라이브러리
- `@expo/vector-icons`: ^15.0.3 - 벡터 아이콘
- `react-native-safe-area-context`: ~5.6.0 - Safe Area 처리
- `react-native-reanimated`: ~4.1.1 - 애니메이션
- `react-native-worklets`: 0.5.1 - 워크렛

#### 유틸리티
- `date-fns`: ^4.1.0 - 날짜 처리
- `@react-native-community/datetimepicker`: ^8.5.1 - 날짜/시간 선택기

#### 개발 도구
- `typescript`: ~5.9.2 - TypeScript 컴파일러
- `eslint`: ^9.25.0 - 코드 린터
- `eslint-config-expo`: ~10.0.0 - Expo ESLint 설정
- `@types/react`: ~19.1.0 - React 타입 정의

---

## 📱 모바일 앱 (실기기 테스트용)

### Expo Go 앱 설치
- **iOS**: App Store에서 "Expo Go" 검색
- **Android**: Google Play Store에서 "Expo Go" 검색

---

## 💻 개발 도구 (선택사항)

### VS Code 확장 프로그램 (권장)
1. **ES7+ React/Redux/React-Native snippets**
2. **React Native Tools**
3. **Expo Tools**
4. **ESLint**
5. **Prettier - Code formatter**

### Android Studio (Android 에뮬레이터용)
- **다운로드**: https://developer.android.com/studio
- Android SDK 및 에뮬레이터 설정 필요

### Xcode (iOS 시뮬레이터용, Mac만)
- App Store에서 설치
- macOS에서만 사용 가능

---

## 🚀 프로젝트 실행

### 1. 개발 서버 시작
```bash
npm start
# 또는
npx expo start
```

### 2. 실행 옵션
- **웹 브라우저**: 터미널에서 `w` 키 누르기
- **Android 에뮬레이터**: 터미널에서 `a` 키 누르기 (Android Studio 필요)
- **iOS 시뮬레이터**: 터미널에서 `i` 키 누르기 (Mac + Xcode 필요)
- **실기기**: Expo Go 앱으로 QR 코드 스캔

### 3. 캐시 클리어 후 시작 (문제 발생 시)
```bash
npx expo start --clear
```

### 4. 터널 모드 (다른 Wi-Fi 네트워크에서 접속)
```bash
npx expo start --tunnel
```

---

## 🔧 추가 설정 (선택사항)

### 환경 변수 설정
프로젝트 루트에 `.env` 파일 생성:
```
API_BASE_URL=http://your-server.com/api
```

### Git 설정 (버전 관리)
```bash
git init
git add .
git commit -m "Initial commit"
```

---

## ✅ 설치 확인 체크리스트

- [ ] Node.js 18+ 설치 확인 (`node --version`)
- [ ] npm 설치 확인 (`npm --version`)
- [ ] Expo CLI 설치 확인 (`expo --version`)
- [ ] 프로젝트 폴더에서 `npm install` 실행 완료
- [ ] `npm start` 실행 성공
- [ ] Expo Go 앱 설치 (실기기 테스트용)

---

## 🐛 문제 해결

### 패키지 설치 오류
```bash
# node_modules 삭제 후 재설치
rm -rf node_modules
npm install
```

### 캐시 문제
```bash
# Expo 캐시 클리어
npx expo start --clear

# npm 캐시 클리어
npm cache clean --force
```

### 포트 충돌
```bash
# 다른 포트로 실행
npx expo start --port 8082
```

### 네트워크 연결 문제
```bash
# 터널 모드 사용
npx expo start --tunnel
```

---

## 📚 참고 자료

- **Expo 공식 문서**: https://docs.expo.dev/
- **React Navigation**: https://reactnavigation.org/
- **React Native 문서**: https://reactnative.dev/
- **Lucide Icons**: https://lucide.dev/

---

## 🎯 빠른 시작 명령어 요약

```bash
# 1. 프로젝트 폴더로 이동
cd firedrone

# 2. 의존성 설치
npm install

# 3. 개발 서버 시작
npm start

# 4. 웹에서 실행하려면 터미널에서 'w' 키
# 5. 실기기에서 실행하려면 Expo Go 앱으로 QR 코드 스캔
```

---

**설치가 완료되면 `npm start`로 프로젝트를 실행하세요!** 🚀

